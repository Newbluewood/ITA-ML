# ITA-ML — vođeni ML projekat (Kurs 10, lekcije 12–19)

**Kurs:** Introduction to Machine Learning using Python  
**Autor:** Nebojša Simović

Repozitorijum za **lekcijski** projekat: EDA → čišćenje → feature engineering → model → validacija → `joblib` (sentiment na recenzijama, `product_reviews_full.csv`).

> **Finalni Task 3 (947)** — predikcija kategorije iz `products.csv` — biće u **novom** GitHub repou. Ovde samo pratimo lekcije.

## Struktura

```
├── data/
│   └── IMLP4_13-product_reviews_full.csv
├── notebooks/
├── model/
├── requirements.txt
└── README.md
```

## Okruženje

```powershell
cd ITA-ML
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

## Kloniranje

```bash
git clone https://github.com/Newbluewood/ITA-ML.git
```

SSH:

```bash
git clone git@github.com-blw:Newbluewood/ITA-ML.git
```

**Autor:** Nebojša Simović
